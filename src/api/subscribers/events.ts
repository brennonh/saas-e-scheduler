/**
 * events
 * ---------------------
 * Define all your possible custom events here.
 */
export const events = {
    gcp: {
        pubsub: {
            subscribe: 'subscribe',
            error: 'error',
            lifecycle: 'lifecycle',
        },
    },
};
